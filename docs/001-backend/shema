-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.accounts
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    iban character varying COLLATE pg_catalog."default",
    balance numeric(12, 2),
    active boolean NOT NULL,
    user_id integer NOT NULL,
    bank_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    currency currencyenum NOT NULL,
    CONSTRAINT accounts_pkey PRIMARY KEY (id),
    CONSTRAINT uq_accounts_iban UNIQUE (iban)
);

CREATE TABLE IF NOT EXISTS public.alembic_version
(
    version_num character varying(32) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT alembic_version_pkc PRIMARY KEY (version_num)
);

CREATE TABLE IF NOT EXISTS public.banks
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    cif character varying COLLATE pg_catalog."default",
    CONSTRAINT banks_pkey PRIMARY KEY (id),
    CONSTRAINT uq_banks_cif UNIQUE (cif)
);

CREATE TABLE IF NOT EXISTS public.cards
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    last4 character varying(4) COLLATE pg_catalog."default",
    type cardtypeenum NOT NULL,
    active boolean NOT NULL,
    account_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT cards_pkey PRIMARY KEY (id),
    CONSTRAINT uq_card_account_last4 UNIQUE (account_id, name, last4)
);

CREATE TABLE IF NOT EXISTS public.category_rules
(
    id serial NOT NULL,
    name character varying(255) COLLATE pg_catalog."default" NOT NULL,
    pattern character varying(1000) COLLATE pg_catalog."default" NOT NULL,
    type transactionenum NOT NULL,
    priority integer NOT NULL,
    is_active boolean NOT NULL,
    category_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT category_rules_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.expenses
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    amount numeric(12, 2) NOT NULL,
    date date NOT NULL,
    currency currencyenum NOT NULL,
    source_id integer NOT NULL,
    category_id integer NOT NULL,
    account_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    dedup_hash character varying(64) COLLATE pg_catalog."default" NOT NULL,
    ignore_in_analysis boolean NOT NULL,
    card_id integer,
    CONSTRAINT expenses_pkey PRIMARY KEY (id),
    CONSTRAINT uq_expenses_account_dedup_hash UNIQUE (account_id, dedup_hash)
);

CREATE TABLE IF NOT EXISTS public.expenses_categories
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT expenses_categories_pkey PRIMARY KEY (id),
    CONSTRAINT uq_expense_category_name UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS public.households
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    address character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT households_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.households_members
(
    id serial NOT NULL,
    role roleenum NOT NULL,
    active boolean NOT NULL,
    household_id integer NOT NULL,
    user_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT households_members_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.incomes
(
    id serial NOT NULL,
    description character varying COLLATE pg_catalog."default" NOT NULL,
    amount numeric(12, 2) NOT NULL,
    date date NOT NULL,
    currency currencyenum NOT NULL,
    source_id integer NOT NULL,
    category_id integer NOT NULL,
    account_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    dedup_hash character varying(64) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT incomes_pkey PRIMARY KEY (id),
    CONSTRAINT uq_incomes_account_dedup_hash UNIQUE (account_id, dedup_hash)
);

CREATE TABLE IF NOT EXISTS public.incomes_categories
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT incomes_categories_pkey PRIMARY KEY (id),
    CONSTRAINT uq_income_category_name UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS public.investments
(
    id serial NOT NULL,
    date date NOT NULL,
    currency currencyenum NOT NULL,
    account_id integer NOT NULL,
    category_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    description character varying COLLATE pg_catalog."default" NOT NULL,
    amount numeric(12, 2) NOT NULL,
    dedup_hash character varying(64) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT investments_pkey PRIMARY KEY (id),
    CONSTRAINT uq_investments_account_dedup_hash UNIQUE (account_id, dedup_hash)
);

CREATE TABLE IF NOT EXISTS public.investments_categories
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT investments_categories_pkey PRIMARY KEY (id),
    CONSTRAINT uq_investment_category_name UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS public.investments_logs
(
    id serial NOT NULL,
    date date NOT NULL,
    current_value numeric(12, 2) NOT NULL,
    price_per_unit numeric(12, 2),
    units_bought numeric(12, 6),
    action actionenum NOT NULL,
    note character varying COLLATE pg_catalog."default",
    investment_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT investments_logs_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.places
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    address character varying COLLATE pg_catalog."default",
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT places_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.savings
(
    id serial NOT NULL,
    description character varying COLLATE pg_catalog."default" NOT NULL,
    amount numeric(12, 2) NOT NULL,
    date date NOT NULL,
    currency currencyenum NOT NULL,
    account_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT savings_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.savings_logs
(
    id serial NOT NULL,
    date date NOT NULL,
    amount numeric(12, 2) NOT NULL,
    total_amount numeric(12, 2),
    note character varying COLLATE pg_catalog."default",
    saving_id integer NOT NULL,
    source_id integer NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT savings_logs_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.sources
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    description character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    type sourcetypeenum NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT sources_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.users
(
    id serial NOT NULL,
    name character varying COLLATE pg_catalog."default" NOT NULL,
    surname1 character varying COLLATE pg_catalog."default" NOT NULL,
    surname2 character varying COLLATE pg_catalog."default",
    dni character varying COLLATE pg_catalog."default" NOT NULL,
    email character varying COLLATE pg_catalog."default",
    active boolean NOT NULL,
    telephone integer,
    password character varying COLLATE pg_catalog."default" NOT NULL,
    role userroleenum NOT NULL,
    created_at timestamp without time zone NOT NULL DEFAULT now(),
    updated_at timestamp without time zone NOT NULL DEFAULT now(),
    deleted_at timestamp without time zone,
    CONSTRAINT users_pkey PRIMARY KEY (id),
    CONSTRAINT uq_users_dni UNIQUE (dni)
);

ALTER TABLE IF EXISTS public.accounts
    ADD CONSTRAINT accounts_bank_id_fkey FOREIGN KEY (bank_id)
    REFERENCES public.banks (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_accounts_bank_id
    ON public.accounts(bank_id);


ALTER TABLE IF EXISTS public.accounts
    ADD CONSTRAINT accounts_user_id_fkey FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_accounts_user_id
    ON public.accounts(user_id);


ALTER TABLE IF EXISTS public.cards
    ADD CONSTRAINT cards_account_id_fkey FOREIGN KEY (account_id)
    REFERENCES public.accounts (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.expenses
    ADD CONSTRAINT expenses_account_id_fkey FOREIGN KEY (account_id)
    REFERENCES public.accounts (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_expenses_account_id
    ON public.expenses(account_id);


ALTER TABLE IF EXISTS public.expenses
    ADD CONSTRAINT expenses_card_id_fkey FOREIGN KEY (card_id)
    REFERENCES public.cards (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.expenses
    ADD CONSTRAINT expenses_category_id_fkey FOREIGN KEY (category_id)
    REFERENCES public.expenses_categories (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_expenses_category_id
    ON public.expenses(category_id);


ALTER TABLE IF EXISTS public.expenses
    ADD CONSTRAINT expenses_source_id_fkey FOREIGN KEY (source_id)
    REFERENCES public.sources (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_expenses_source_id
    ON public.expenses(source_id);


ALTER TABLE IF EXISTS public.households_members
    ADD CONSTRAINT households_members_household_id_fkey FOREIGN KEY (household_id)
    REFERENCES public.households (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_households_members_household_id
    ON public.households_members(household_id);


ALTER TABLE IF EXISTS public.households_members
    ADD CONSTRAINT households_members_user_id_fkey FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_households_members_user_id
    ON public.households_members(user_id);


ALTER TABLE IF EXISTS public.incomes
    ADD CONSTRAINT incomes_account_id_fkey FOREIGN KEY (account_id)
    REFERENCES public.accounts (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_incomes_account_id
    ON public.incomes(account_id);


ALTER TABLE IF EXISTS public.incomes
    ADD CONSTRAINT incomes_category_id_fkey FOREIGN KEY (category_id)
    REFERENCES public.incomes_categories (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_incomes_category_id
    ON public.incomes(category_id);


ALTER TABLE IF EXISTS public.incomes
    ADD CONSTRAINT incomes_source_id_fkey FOREIGN KEY (source_id)
    REFERENCES public.sources (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_incomes_source_id
    ON public.incomes(source_id);


ALTER TABLE IF EXISTS public.investments
    ADD CONSTRAINT investments_account_id_fkey FOREIGN KEY (account_id)
    REFERENCES public.accounts (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_investments_account_id
    ON public.investments(account_id);


ALTER TABLE IF EXISTS public.investments
    ADD CONSTRAINT investments_category_id_fkey FOREIGN KEY (category_id)
    REFERENCES public.investments_categories (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_investments_category_id
    ON public.investments(category_id);


ALTER TABLE IF EXISTS public.investments_logs
    ADD CONSTRAINT investments_logs_investment_id_fkey FOREIGN KEY (investment_id)
    REFERENCES public.investments (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_investments_logs_investment_id
    ON public.investments_logs(investment_id);


ALTER TABLE IF EXISTS public.savings
    ADD CONSTRAINT savings_account_id_fkey FOREIGN KEY (account_id)
    REFERENCES public.accounts (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_savings_account_id
    ON public.savings(account_id);


ALTER TABLE IF EXISTS public.savings_logs
    ADD CONSTRAINT savings_logs_saving_id_fkey FOREIGN KEY (saving_id)
    REFERENCES public.savings (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_savings_logs_saving_id
    ON public.savings_logs(saving_id);


ALTER TABLE IF EXISTS public.savings_logs
    ADD CONSTRAINT savings_logs_source_id_fkey FOREIGN KEY (source_id)
    REFERENCES public.sources (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;
CREATE INDEX IF NOT EXISTS ix_savings_logs_source_id
    ON public.savings_logs(source_id);

END;